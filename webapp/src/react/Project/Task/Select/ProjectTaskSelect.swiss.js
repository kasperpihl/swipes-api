import { styleSheet } from 'swiss-react';

export default styleSheet('ProjectTaskSelect', {
  Wrapper: {
    _size: ['24px', '24px'],
    _flex: 'center',
    flex: 'none',
    cursor: 'pointer',
    userSelect: 'none'
  }
});
