import { styleSheet } from 'swiss-react';

export default styleSheet('ProjectTaskList', {
  Wrapper: {
    _size: ['100%', 'auto'],
    _flex: ['column', 'flex-start', 'flex-start'],
    padding: '6px 0'
  }
});
