import { styleSheet } from 'swiss-react';

export default styleSheet('CompatibleSubHeader', {
  Wrapper: {
    _el: 'h4',
    _font: ['24px', '24px', '300'],
    color: '$sw1',
    paddingTop: '45px',
  }
});
