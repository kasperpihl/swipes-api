import { styleSheet } from 'swiss-react';

export default styleSheet('DiscussionList', {
  Wrapper: {
    _size: '100%',
    overflowY: 'scroll',
  },
  ActonBar: {
    _size: ['100%', '60px'],
  },
});
