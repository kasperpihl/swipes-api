import { styleSheet } from 'react-swiss';

export default styleSheet('Audio', {
  Wrapper: {
    _size: '100%',
    _flex: ['row', 'center', 'center'],
    maxHeight: '99%',
    maxWidth: '90vw',
  }
});