import { styleSheet } from 'swiss-react';

export default styleSheet('GoalFooter', {
  Wrapper: {
    _size: ['100%', '54px'],
    borderTop: '1px solid $sw3',
    _flex: ['row', 'left', 'center'],
    padding: '9px 12px',
  },
});