import { styleSheet } from 'swiss-react';

export default styleSheet('PlanSide', {
  Wrapper: {
    _size: ['180px', 'auto'],
    _flex: ['column', 'left', 'top'],
    flex: 'none',
    marginRight: '48px'
  }
});
