import { styleSheet } from 'swiss-react';

export default styleSheet('PlanSideDraft', {
  Wrapper: {
    _size: ['180px', 'auto'],
    _flex: ['column', 'left', 'top'],
    flex: 'none',
    marginRight: '48px'
  }
});
