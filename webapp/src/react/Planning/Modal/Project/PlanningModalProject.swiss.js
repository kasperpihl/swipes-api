import { styleSheet } from 'swiss-react';

export default styleSheet('PlanningModalProject', {
  Wrapper: {
    overflowY: 'auto',
    width: '100%',
    height: '100%',
  },
  LoaderWrapper: {
    _flex: 'center',
    height: '100%',
    width: '100%',
    padding: '12px 0'
  }
});
