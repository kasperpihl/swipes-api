import { styleSheet } from 'swiss-react';

export default styleSheet('FileAudio', {
  Wrapper: {
    _size: '100%',
    _flex: ['row', 'center', 'center'],
    maxHeight: '99%',
    maxWidth: '90vw'
  }
});
