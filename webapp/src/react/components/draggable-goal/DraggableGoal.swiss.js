import { styleSheet } from 'react-swiss';

export default styleSheet('DraggableGoal', {
  Wrapper: {
    _size: 'auto',
    background: '$sw3',
    borderRadius: '3px',
    marginBottom: '3px',
  }
});