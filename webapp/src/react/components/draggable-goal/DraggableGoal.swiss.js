import { styleSheet } from 'swiss-react';

export default styleSheet('DraggableGoal', {
  Wrapper: {
    _size: 'auto',
    background: '$sw3',
    borderRadius: '3px',
    marginBottom: '3px',
  }
});